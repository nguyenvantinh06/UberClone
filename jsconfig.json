{
    "compilerOptions": {
      "target": "es6",
      "module": "commonjs",
      "baseUrl": ".",        // Đặt base url là thư mục gốc là thư mục hiện tại, không nên đặt baseUrl là ./src vì sau này dễ nhầm lần giữa import component và import thư viện
      "paths": {
        "~/*": ["./src/*"]     // Đặt "~" là alias cho thư mục src, ~ chính là thư mục src
      },
    },
    "exclude": [
      "node_modules",        // Đừng bắt VSCode nó phải chọc vào
    ]                        // cái đống trong này.
  }